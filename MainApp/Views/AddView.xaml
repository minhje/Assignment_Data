<UserControl x:Class="MainApp.Views.AddView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MainApp.Views"
             xmlns:viewmodels="clr-namespace:MainApp.ViewModels"
             xmlns:views="clr-namespace:MainApp.Views"
             d:DataContext="{d:DesignInstance Type=viewmodels:AddViewModel}"
             
             mc:Ignorable="d" 
             Background="#FFFFFF"
             d:DesignHeight="450" d:DesignWidth="800">

    <Border>
        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/> 
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="New project" Style="{StaticResource Heading}"/>
        </Grid>
    </Border>
</UserControl>
